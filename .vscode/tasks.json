{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build All",
            "type": "shell",
            "command": [
                "make",
                "compile"
            ],
            "options": {
                "cwd": "${workspaceFolder}"
            }
        },
        {
          "label": "fix_gdbinit",
          "type": "shell",
          "command": "sed -i -e '/^target remote/d' ${workspaceFolder}/.gdbinit"
      }
    ]
}